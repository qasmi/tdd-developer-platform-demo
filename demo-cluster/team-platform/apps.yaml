- name: crossplane
  owner: platform
  project: platform
  namespace: crossplane
  repo: https://charts.crossplane.io/stable
  chart: crossplane
  version: "1.21.*"
  valueFiles:
    - crossplane.yaml
  autoSync:
    enabled: true
