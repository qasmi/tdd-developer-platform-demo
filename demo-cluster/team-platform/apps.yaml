- name: crossplane
  owner: platform
  project: platform
  namespace: crossplane
  repo: https://charts.crossplane.io/stable
  chart: crossplane
  version: "2.0.2"
  valueFiles:
    - crossplane.yaml
  autoSync:
    enabled: true
