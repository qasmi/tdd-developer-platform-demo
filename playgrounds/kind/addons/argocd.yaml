global:
  logging:
    format: json

configs:
  secret:
    argocdServerAdminPassword: $2a$10$QeCKgnhmMIAYD/SRZnnpc.iDSVT0fJ8o5C7p49Ko6nbv/Er6hyukG
  rbac:
    create: true
    scopes: '[email,groups]'
    policy.default: role:readonly
    policy.csv: |
      p, role:readonly, applications, get, */*, allow
      p, role:admin, applications, *, */*, allow

applicationSet:
  replicas: 1
      
controller:
  extraArgs:
    - --app-resync=20
  replicas: 1
  enableStatefulSet: true
  logFormat: json

redis-ha:
  enabled: false
  
repoServer:
  logFormat: json
  logLevel: "warn"

server:
  extraArgs:
    - --insecure
  service:
    type: LoadBalancer
    loadBalancerIP: "172.18.250.0"
  logFormat: json
      
dex:
  enabled: false

notifications:
  enabled: false